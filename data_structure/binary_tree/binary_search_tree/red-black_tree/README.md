# 红黑树

一种**自平衡**的**二叉搜素树**（BST）。

## 结构

满足以下性质的二叉搜索树：
1. 节点颜色都是**红色或黑色**。
2. **根节点**必须是**黑色**。
3. **叶子节点**是**黑色**。
4. **红色节点**的两个**子节点**必须是**黑色**。
5. 从任意节点到其后代叶子节点的路径上，黑色节点数相同（**黑高**）。

保证了红黑树不会严重偏斜，树高始终 ≤ `2*log2(n+1)`。